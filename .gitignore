# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node (按需)
node_modules/

# 插件打包产物
*.eagleplugin
*.zip
__MACOSX/

# Build artifact（每次 npm run build 重新生成）
js/bundle.js

# 其他工具在工作目录里产生的残留（playwright session 日志、图表 / 截图等），
# 不属于本项目，禁止入库
.playwright-mcp/
/barchart_*.json
/map-*.png
/polish-*.png
/ux-*.png

# Eagle 调试残留
*.log

# 模板母版（不入项目仓库，仅本地保留供 relearning 读写）
AGENT.template.md

# 过程证据（截图等，不入库）
.agent-doc/evidence/
.agent-doc/drafts/*.tmp

# 环境变量与密钥（本项目当前不用，预留）
.env
.env.*
!.env.example
